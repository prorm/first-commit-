{
  "name": "sentryshield",
  "version": "1.0.0",
  "private": true,
  "description": "SentryShield - phone-based acoustic perception and spatial reconstruction prototype",
  "type": "commonjs",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "lint": "node --experimental-vm-modules scripts/check-syntax.js",
    "verify": "npm run lint && npm test && node scripts/browser-check.mjs",
    "eval": "node scripts/verify-classifier.mjs && node scripts/eval-reconstruction.js && node scripts/window-stats.mjs",
    "browser": "node scripts/browser-check.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.658.0",
    "@aws-sdk/client-polly": "^3.658.0"
  }
}