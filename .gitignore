node_modules/
.env
# Scan recordings are demo artefacts: keep the directory, not the contents.
# Real phone training datasets and the reference scan are committed deliberately.
recordings/*.json
!recordings/reference-*.json
!recordings/real_*.json

# Browser-check and hero screenshots are regenerated on demand.
scripts/shot-*.png
scripts/hero-*.png
